/*Intro to ADT Stack*/
#include <stdio.h>
int main(void)
{
/*
The Abstract Data Type stack (ADT)
> canonical non-trivial data structure: appears in both computer algorithms and in computer architecture

Operations: push, pop, top, empty, full, reset

typedef struct stack{
    char s [MAX_LEN];
    int top;
} stack;

//underlying rep is an array

push

    void push (char c, stack * stk)
    {
       stk -> top++;
       stk -> s[stk->top] = c;
    }


*/

    return 0;
}