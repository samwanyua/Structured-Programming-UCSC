/*Intro to File I/O*/

#include <stdio.h>
int main(void)
{
/*
Chapter 11
fscanf(FILE->fp, ...) //pointer to  a file
so it is like scanf but needs  a named file

Opening a file for reading
    ifp = fopen ("filename", "r"); //r = reading the file
    //ifp is input file pointer declared as 
        FILE *ifp

    To close;
        f(close(ifp));
    

*/

    return 0;
}